{% extends 'layout.html.twig' %}

{% block container %}
    <div class="row">
        <div class="col-xs-4 col-xs-offset-4 error">
            <h2>Pagina no encontrada</h2>
            <h3>Vaya, parece que hubo un error.</h3>
            <h4>La pagina {{ app.request.getRequestUri }} no existe.</h4>
        </div>
    </div>
{% endblock %}